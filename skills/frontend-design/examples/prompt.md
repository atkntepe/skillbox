# Example Prompt

Use the `frontend-design` skill.

Improve this dashboard screen so it feels more polished, responsive, and production-ready. Keep the current tech stack and avoid adding unnecessary dependencies.
